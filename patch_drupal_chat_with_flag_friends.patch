diff --git a/drupalchat.module b/drupalchat.module
index 8d1c5f4..e4eeb21 100755
--- a/drupalchat.module
+++ b/drupalchat.module
@@ -897,7 +897,7 @@ function _drupalchat_buddylist($uid) {
     }
   }
   elseif ($drupalchat_rel == DRUPALCHAT_REL_FF) {
-    $result = db_query("SELECT * FROM {flag_friend} WHERE uid = :uid OR entity_id = :uid", array(':uid' => $uid, ':friend_uid' => $uid));
+    $result = db_query("SELECT * FROM {flag_friend} WHERE (uid = :uid OR entity_id = :uid) AND  status = 1", array(':uid' => $uid, ':friend_uid' => $uid));
 
     foreach ($result as $friend) {
       $uid == $friend->uid ? $users[] = $friend->friend_uid : $users[] = $friend->uid;
